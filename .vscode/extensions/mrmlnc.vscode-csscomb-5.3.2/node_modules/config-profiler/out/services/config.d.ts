import { Cache, IOptions } from '../types';
export interface IBuildedConfig {
    extends: string;
}
export declare function include(cache: Cache, filepath: string, options: IOptions): Promise<IBuildedConfig>;
