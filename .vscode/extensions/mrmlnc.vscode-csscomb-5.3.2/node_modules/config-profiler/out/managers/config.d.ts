import { ConfigType, IConfig, IResult, IOptions } from '../types';
export declare function prepare(type: ConfigType, path: string, ctime: number, config: object): IConfig;
export declare function build(type: ConfigType, path: string, config: object, options: IOptions): IResult;
