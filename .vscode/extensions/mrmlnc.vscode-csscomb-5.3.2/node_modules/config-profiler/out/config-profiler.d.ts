import { IOptions, IChangeableOptions, IResult } from './types';
export default class ConfigProfiler {
    private workspace;
    private options;
    private readonly cache;
    constructor(workspace: string, options: IOptions);
    setWorkspace(workspace: string): void;
    setOptions(options: IOptions): void;
    getConfig(filepath: string, options?: IChangeableOptions): Promise<IResult>;
}
