'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const assert = require("assert");
const io = require("./io");
describe('Utils → IO', () => {
    const cwd = process.cwd().replace(/\\/g, '/');
    describe('.findUp', () => {
        it('should return path to the config file', () => tslib_1.__awaiter(this, void 0, void 0, function* () {
            const expected = `${cwd}/fixtures/io/config.json`;
            const actual = yield io.findUp(['config.json'], './fixtures/io/nested/current/file.txt', './fixtures/io');
            assert.equal(actual, expected);
        }));
        it('should return null', () => tslib_1.__awaiter(this, void 0, void 0, function* () {
            const expected = null;
            const actual = yield io.findUp(['wow.json'], './fixtures/io/nested/current/file.txt', './fixtures/io');
            assert.equal(actual, expected);
        }));
    });
});
